export class Ingredient {
  // public name: string;
  // public amount: number;
  constructor(public name: string, public amount: number) { // adding an accessor like public before the name or amount value is a shortcut
  }

}
